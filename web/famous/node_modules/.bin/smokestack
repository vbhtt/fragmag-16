#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../smokestack/bin/smokestack.js" "$@"
  ret=$?
else 
  node  "$basedir/../smokestack/bin/smokestack.js" "$@"
  ret=$?
fi
exit $ret
